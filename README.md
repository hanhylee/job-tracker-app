# Job Tracker

Monorepo for a job application tracker.

## API

The backend is a Cloudflare Worker in [`workers/job-tracker`](workers/job-tracker).

```bash
npm install
npm run deploy          # from repo root
```
